<%= fetch(:rails_env) %>:
  adapter: postgresql
  timeout: 5000
  encoding: utf8
  reconnect: false
  database: <%= "#{fetch(:application)}" %>
  pool: 5
  username: <%= "#{fetch(:application)}" %>
  password: <%= "#{fetch(:db_password)}"%>
  host:
  port: 5432
